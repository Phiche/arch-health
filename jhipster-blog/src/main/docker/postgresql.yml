version: '2'
services:
  dossier-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/Dossier/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=Dossier
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
